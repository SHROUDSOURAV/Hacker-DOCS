const nextConfig = {
  // Use Vercel's default deployment builder. This fixes dev mode parsing issues
  // while ensuring lightning fast deployed statically rendered routes.
};

export default nextConfig;
